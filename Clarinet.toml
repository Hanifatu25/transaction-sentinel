[project]
name = "transaction-sentinel"
authors = ["Transaction Sentinel Team"]
description = "A comprehensive transaction tracking and verification smart contract"
telemetry = false
requirements = []
[contracts.transaction-monitor]
path = "contracts/transaction-monitor.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
